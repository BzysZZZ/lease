<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pzj.lease.web.admin.mapper.AttrValueMapper">

    <select id="seletListByRoomId" resultType="com.pzj.lease.web.admin.vo.attr.AttrValueVo">
        SELECT
            av.id,
            av.name,
            av.attr_key_id,
            ak.name
            FROM room_attr_value rav
        LEFT JOIN attr_value av
        ON av.is_deleted=0
        AND rav.attr_value_id=av.id
        LEFT JOIN attr_key ak
        ON ak.is_deleted=0
        AND av.attr_key_id=ak.id
        WHERE rav.room_id=#{id}
    </select>
</mapper>
